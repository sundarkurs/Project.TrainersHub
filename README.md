## Project.TrainersHub

It's a sample project to demonstrate ASP.Net Core and ReactJS to build an web application.

It has two separate projects as below,
1. ASP.Net Core to expose REST API's
2. ReactJS project for front-end development
